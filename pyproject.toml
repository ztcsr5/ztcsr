[project]
name = "ztcsr-music-app"
version = "0.1.0"
description = "Advanced command-line music software with playlist and recommendation features"
authors = [{name = "AutoGenerated"}]
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=67"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
